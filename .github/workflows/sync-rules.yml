name: Sync Rules

on:
  schedule:
    - cron: '0 */6 * * *' # 每 6 小时自动执行一次
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'scripts/sync-rules.js'

concurrency:
  group: shadowstore-data-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: |
          if [ -f package.json ]; then
            npm ci
          fi

      - name: Run Rules Synchronization Script
        run: node scripts/sync-rules.js

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore(rules): 自动同步规则与数据 [skip ci]" && git push)
